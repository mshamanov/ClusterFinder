/**
 * Class that represents a point (cell) of a field
 */
class Point {
    /**
     * @param x coordinate
     * @param y coordinate
     */
    constructor(readonly x: number, readonly y: number) {
    }
}

export default Point;